---
title: Timer
description: A timer component that can record elapsed time or countdown from a target time.
---

import { Timer, TimerArea, TimerItem, TimerSeparator, TimerControl, TimerActionTrigger } from "@/registry/thornberry/components/timer";

<Timer targetMs={60000} className="mb-8">
  <TimerArea>
    <TimerItem type="minutes" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="seconds" />
  </TimerArea>
  <TimerControl>
    <TimerActionTrigger action="start">Start</TimerActionTrigger>
    <TimerActionTrigger action="pause">Pause</TimerActionTrigger>
    <TimerActionTrigger action="resume">Resume</TimerActionTrigger>
  </TimerControl>
</Timer>

## Installation

<InstallationTabs filename="timer.json" />

## Usage

```tsx
import { Timer, TimerArea, TimerItem, TimerSeparator, TimerControl, TimerActionTrigger } from "@/components/ui/timer";
```

```tsx
<Timer targetMs={60000}>
  <TimerArea>
    <TimerItem type="minutes" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="seconds" />
  </TimerArea>
  <TimerControl>
    <TimerActionTrigger action="start">Start</TimerActionTrigger>
    <TimerActionTrigger action="pause">Pause</TimerActionTrigger>
  </TimerControl>
</Timer>
```

## Examples

<br />

#### Sizes

<Tabs items={["Small", "Medium", "Large"]} className="mt-4">
  <Tab value="Small">
    <Timer size="sm" targetMs={30000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>

    ```tsx
    <Timer size="sm" targetMs={30000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>
    ```
  </Tab>
  <Tab value="Medium">
    <Timer size="md" targetMs={30000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>

    ```tsx
    <Timer size="md" targetMs={30000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>
    ```
  </Tab>
  <Tab value="Large">
    <Timer size="lg" targetMs={30000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>

    ```tsx
    <Timer size="lg" targetMs={30000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>
    ```
  </Tab>
</Tabs>

#### Variants

<Tabs items={["Default", "Outlined", "Minimal"]} className="mt-4">
  <Tab value="Default">
    <Timer variant="default" targetMs={45000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>

    ```tsx
    <Timer variant="default" targetMs={45000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>
    ```
  </Tab>
  <Tab value="Outlined">
    <Timer variant="outlined" targetMs={45000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>

    ```tsx
    <Timer variant="outlined" targetMs={45000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>
    ```
  </Tab>
  <Tab value="Minimal">
    <Timer variant="minimal" targetMs={45000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>

    ```tsx
    <Timer variant="minimal" targetMs={45000}>
      <TimerArea>
        <TimerItem type="minutes" />
        <TimerSeparator>:</TimerSeparator>
        <TimerItem type="seconds" />
      </TimerArea>
      <TimerControl>
        <TimerActionTrigger action="start">Start</TimerActionTrigger>
      </TimerControl>
    </Timer>
    ```
  </Tab>
</Tabs>

#### Countdown Timer

<Timer countdown targetMs={120000} className="mt-4">
  <TimerArea>
    <TimerItem type="minutes" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="seconds" />
  </TimerArea>
  <TimerControl>
    <TimerActionTrigger action="start">Start Countdown</TimerActionTrigger>
    <TimerActionTrigger action="pause">Pause</TimerActionTrigger>
  </TimerControl>
</Timer>

```tsx
<Timer countdown targetMs={120000}>
  <TimerArea>
    <TimerItem type="minutes" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="seconds" />
  </TimerArea>
  <TimerControl>
    <TimerActionTrigger action="start">Start Countdown</TimerActionTrigger>
    <TimerActionTrigger action="pause">Pause</TimerActionTrigger>
  </TimerControl>
</Timer>
```

#### Full Time Format

<Timer targetMs={3661000} className="mt-4">
  <TimerArea>
    <TimerItem type="hours" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="minutes" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="seconds" />
  </TimerArea>
  <TimerControl>
    <TimerActionTrigger action="start">Start</TimerActionTrigger>
    <TimerActionTrigger action="pause">Pause</TimerActionTrigger>
    <TimerActionTrigger action="resume">Resume</TimerActionTrigger>
  </TimerControl>
</Timer>

```tsx
<Timer targetMs={3661000}>
  <TimerArea>
    <TimerItem type="hours" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="minutes" />
    <TimerSeparator>:</TimerSeparator>
    <TimerItem type="seconds" />
  </TimerArea>
  <TimerControl>
    <TimerActionTrigger action="start">Start</TimerActionTrigger>
    <TimerActionTrigger action="pause">Pause</TimerActionTrigger>
    <TimerActionTrigger action="resume">Resume</TimerActionTrigger>
  </TimerControl>
</Timer>
```
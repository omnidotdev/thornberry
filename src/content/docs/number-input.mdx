---
title: Number Input
description: A form control that allows users to input numeric values with increment and decrement buttons
---

import {
  NumberInput,
  NumberInputContext,
  NumberInputControl,
  NumberInputDecrementTrigger,
  NumberInputIncrementTrigger,
  NumberInputInput,
  NumberInputLabel,
  NumberInputProvider,
  NumberInputRoot,
} from "@/registry/thornberry/components/number-input";

## Basic

<NumberInput />

## With Label

<NumberInput label="Quantity" />

## With Placeholder

<NumberInput placeholder="Enter a number" />

## Custom Range

<NumberInput
  label="Temperature (°C)"
  min={-10}
  max={40}
  defaultValue={22}
/>

## Custom Step

<NumberInput
  label="Volume"
  min={0}
  max={100}
  step={5}
  defaultValue={50}
/>

## Disabled

<NumberInput
  label="Disabled Input"
  disabled
  defaultValue={10}
/>

## Primitives

<NumberInputRoot min={0} max={100} step={10} defaultValue={30}>
  <NumberInputLabel className="text-blue-700">Custom Number Input</NumberInputLabel>
  <NumberInputControl className="border-blue-300">
    <NumberInputDecrementTrigger className="bg-blue-50 hover:bg-blue-100 text-blue-700" />
    <NumberInputInput className="text-center font-medium text-blue-700" />
    <NumberInputIncrementTrigger className="bg-blue-50 hover:bg-blue-100 text-blue-700" />
  </NumberInputControl>
</NumberInputRoot>

## Format Value

<NumberInputRoot 
  min={0} 
  max={1000} 
  step={100} 
  defaultValue={500}
  formatOptions={{
    style: 'currency',
    currency: 'USD',
    minimumFractionDigits: 0,
    maximumFractionDigits: 0
  }}
>
  <NumberInputLabel>Budget</NumberInputLabel>
  <NumberInputControl>
    <NumberInputDecrementTrigger />
    <NumberInputInput className="text-right" />
    <NumberInputIncrementTrigger />
  </NumberInputControl>
</NumberInputRoot>

## With Description

<div className="space-y-1">
  <NumberInput label="Rating" min={1} max={5} defaultValue={3} />
  <p className="text-xs text-muted-foreground">Rate from 1 to 5 stars</p>
</div>
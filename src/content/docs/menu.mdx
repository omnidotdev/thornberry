---
title: Menu
description: A dropdown menu that displays a list of options.
---

import { FiChevronRight } from "react-icons/fi";

import { Menu, MenuItem } from "@/registry/thornberry/components/menu";
import { Button } from "@/registry/thornberry/components/button";

<Menu
  trigger={<Button variant="outline">Open Menu</Button>}
  className="mb-8"
>
  <MenuItem value="copy">Copy</MenuItem>
  <MenuItem value="paste">Paste</MenuItem>
  <MenuItem value="cut">Cut</MenuItem>
</Menu>

### Installation

<Tabs items={["bun", "pnpm", "yarn", "npm"]} className="mt-4">
  <Tab value="bun">
    ```
    bunx --bun shadcn@latest add http://localhost:3000/r/menu.json
    ```
  </Tab>
  <Tab value="pnpm">
    ```
    pnpm dlx shadcn@latest add http://localhost:3000/r/menu.json
    ```
  </Tab>
  <Tab value="yarn">
    ```
    npx shadcn@latest add http://localhost:3000/r/menu.json
    ```
  </Tab>
  <Tab value="npm">
    ```
    npx shadcn@latest add http://localhost:3000/r/menu.json
    ```
  </Tab>
</Tabs>

### Examples

Let's see how to use the `Menu` component. Let's start with a basic example:

```tsx
import { Menu } from "@/components/ui/menu"
import { Button } from "@/components/ui/button"

export const BasicMenu = () => (
  <Menu trigger={<Button variant="outline">Open Menu</Button>}>
    <Menu.Item value="copy">Copy</Menu.Item>
    <Menu.Item value="paste">Paste</Menu.Item>
    <Menu.Item value="cut">Cut</Menu.Item>
  </Menu>
)
```

You can organize menu items into groups:

```tsx
import { Menu } from "@/components/ui/menu"
import { Button } from "@/components/ui/button"

export const MenuWithGroups = () => (
  <Menu trigger={<Button>Menu with Groups</Button>}>
    <Menu.Group>
      <Menu.GroupLabel>Actions</Menu.GroupLabel>
      <Menu.Item value="copy">Copy</Menu.Item>
      <Menu.Item value="paste">Paste</Menu.Item>
      <Menu.Item value="cut">Cut</Menu.Item>
    </Menu.Group>
    <Menu.Separator />
    <Menu.Group>
      <Menu.GroupLabel>View</Menu.GroupLabel>
      <Menu.Item value="zoom-in">Zoom In</Menu.Item>
      <Menu.Item value="zoom-out">Zoom Out</Menu.Item>
      <Menu.Item value="reset" disabled>Reset View</Menu.Item>
    </Menu.Group>
  </Menu>
)
```

For more granular control, Thornberry provides Menu primitives:

```tsx
import { FiCheck } from "react-icons/fi"

import {
  MenuRoot,
  MenuTrigger,
  MenuPositioner,
  MenuContent,
  MenuItem,
  MenuSeparator,
  MenuItemGroup,
  MenuItemGroupLabel,
  MenuCheckboxItem,
  MenuItemIndicator,
  MenuItemText
} from "@/components/ui/menu"
import { Button } from "@/components/ui/button"

export const MenuPrimitives = () => (
  <MenuRoot>
    <MenuTrigger asChild>
      <Button variant="outline">Custom Menu</Button>
    </MenuTrigger>
    <MenuPositioner>
      <MenuContent>
        <MenuItem value="edit">Edit</MenuItem>
        <MenuItem value="duplicate">Duplicate</MenuItem>
        <MenuSeparator />
        <MenuItemGroup>
          <MenuItemGroupLabel>View Options</MenuItemGroupLabel>
          <MenuCheckboxItem value="grid">
            <MenuItemIndicator>
              <FiCheck className="h-4 w-4" />
            </MenuItemIndicator>
            <MenuItemText>Grid View</MenuItemText>
          </MenuCheckboxItem>
          <MenuCheckboxItem value="list">
            <MenuItemIndicator>
              <FiCheck className="h-4 w-4" />
            </MenuItemIndicator>
            <MenuItemText>List View</MenuItemText>
          </MenuCheckboxItem>
        </MenuItemGroup>
      </MenuContent>
    </MenuPositioner>
  </MenuRoot>
)
```

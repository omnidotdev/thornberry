---
title: Popover
description: A small floating content display that appears when a user interacts with a trigger element.
---

import { PopoverRoot, PopoverTrigger, PopoverPositioner, PopoverContent, PopoverCloseTrigger } from "@/registry/thornberry/components/popover";
import { Button } from "@/registry/thornberry/components/button";
import { Input } from "@/registry/thornberry/components/input";

<PopoverRoot>
  <PopoverTrigger asChild>
    <Button className="mb-8">Open</Button>
  </PopoverTrigger>
  
  <PopoverPositioner>
    <PopoverContent>
      <div className="space-y-2">
        <h4 className="font-medium leading-none">Dimensions</h4>
        <div className="text-sm text-muted-foreground">Set the dimensions for the object.</div>
        <div className="flex gap-2">
          <div className="grid gap-1">
            <label htmlFor="width" className="text-xs">Width</label>
            <Input id="width" defaultValue="100%" className="h-8" />
          </div>
          <div className="grid gap-1">
            <label htmlFor="height" className="text-xs">Height</label>
            <Input id="height" defaultValue="25px" className="h-8" />
          </div>
        </div>
      </div>
    </PopoverContent>
  </PopoverPositioner>
</PopoverRoot>

### Installation

<Tabs items={["bun", "pnpm", "yarn", "npm"]} className="mt-4">
  <Tab value="bun">
    ```
    bunx --bun shadcn@latest add http://localhost:3000/r/popover.json
    ```
  </Tab>
  <Tab value="pnpm">
    ```
    pnpm dlx shadcn@latest add http://localhost:3000/r/popover.json
    ```
  </Tab>
  <Tab value="yarn">
    ```
    npx shadcn@latest add http://localhost:3000/r/popover.json
    ```
  </Tab>
  <Tab value="npm">
    ```
    npx shadcn@latest add http://localhost:3000/r/popover.json
    ```
  </Tab>
</Tabs>

### Examples

Let's see how to use the `Popover` components. Let's start with a basic example:

```tsx
import { 
  PopoverRoot, 
  PopoverTrigger, 
  PopoverPositioner, 
  PopoverContent 
} from "@/components/ui/popover"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"

export const BasicPopover = () => (
  <PopoverRoot>
    <PopoverTrigger asChild>
      <Button>Open</Button>
    </PopoverTrigger>
    
    <PopoverPositioner>
      <PopoverContent>
        Dimensions
        Set the dimensions for the object.
        
        <label htmlFor="width">Width</label>
        <Input id="width" defaultValue="100%" />
        
        <label htmlFor="height">Height</label>
        <Input id="height" defaultValue="25px" />
      </PopoverContent>
    </PopoverPositioner>
  </PopoverRoot>
)
```

You can add a close trigger to the popover content:

```tsx
import { 
  PopoverRoot, 
  PopoverTrigger, 
  PopoverPositioner, 
  PopoverContent, 
  PopoverCloseTrigger 
} from "@/components/ui/popover"
import { Button } from "@/components/ui/button"

export const PopoverWithClose = () => (
  <PopoverRoot>
    <PopoverTrigger asChild>
      <Button>Open</Button>
    </PopoverTrigger>
    
    <PopoverPositioner>
      <PopoverContent>
        Notifications
        <PopoverCloseTrigger />
        
        You have 3 unread messages.
        
        <Button size="sm">View All</Button>
      </PopoverContent>
    </PopoverPositioner>
  </PopoverRoot>
)
```

You can customize the positioning of the popover:

```tsx
import { 
  PopoverRoot, 
  PopoverTrigger, 
  PopoverPositioner, 
  PopoverContent 
} from "@/components/ui/popover"
import { Button } from "@/components/ui/button"

export const PositionedPopovers = () => (
  <>
    <PopoverRoot>
      <PopoverTrigger asChild>
        <Button variant="outline">Left</Button>
      </PopoverTrigger>
      
      <PopoverPositioner placement="left">
        <PopoverContent>
          This popover appears on the left
        </PopoverContent>
      </PopoverPositioner>
    </PopoverRoot>
    
    <PopoverRoot>
      <PopoverTrigger asChild>
        <Button variant="outline">Top</Button>
      </PopoverTrigger>
      
      <PopoverPositioner placement="top">
        <PopoverContent>
          This popover appears on top
        </PopoverContent>
      </PopoverPositioner>
    </PopoverRoot>
    
    <PopoverRoot>
      <PopoverTrigger asChild>
        <Button variant="outline">Bottom</Button>
      </PopoverTrigger>
      
      <PopoverPositioner placement="bottom">
        <PopoverContent>
          This popover appears on the bottom
        </PopoverContent>
      </PopoverPositioner>
    </PopoverRoot>
    
    <PopoverRoot>
      <PopoverTrigger asChild>
        <Button variant="outline">Right</Button>
      </PopoverTrigger>
      
      <PopoverPositioner placement="right">
        <PopoverContent>
          This popover appears on the right
        </PopoverContent>
      </PopoverPositioner>
    </PopoverRoot>
  </>
)
```

```tsx
import {
  PopoverRoot,
  PopoverTrigger,
  PopoverPositioner,
  PopoverContent,
  PopoverArrow,
  PopoverArrowTip,
  PopoverCloseTrigger
} from "@/components/ui/popover"
import { Button } from "@/components/ui/button"

export const CustomStyledPopover = () => (
  <PopoverRoot>
    <PopoverTrigger asChild>
      <Button variant="outline">Open</Button>
    </PopoverTrigger>
    
    <PopoverPositioner>
      <PopoverContent>
        <PopoverArrow>
          <PopoverArrowTip />
        </PopoverArrow>
        
        Custom Styled Popover
        This popover includes an arrow and custom styling.
        
        <PopoverCloseTrigger asChild>
          <Button variant="outline" size="sm">Close</Button>
        </PopoverCloseTrigger>
      </PopoverContent>
    </PopoverPositioner>
  </PopoverRoot>
)
```

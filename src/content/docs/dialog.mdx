---
title: Dialog
description: A window overlaid on the primary window that interrupts the user with important content.
---

import { Button } from "@/registry/thornberry/components/button";
import { DialogRoot, DialogTrigger, DialogBackdrop, DialogPositioner, DialogContent, DialogTitle, DialogDescription, DialogCloseTrigger } from "@/registry/thornberry/components/dialog";

<DialogRoot>
  <DialogTrigger asChild>
    <Button className="mb-8">Open Dialog</Button>
  </DialogTrigger>
  <DialogBackdrop />
  <DialogPositioner>
    <DialogContent>
      <DialogCloseTrigger />
      <DialogTitle>Dialog Title</DialogTitle>
      <DialogDescription>This is a description of the dialog's purpose.</DialogDescription>
      <div className="py-4">This is the main content of the dialog.</div>
      <div className="flex justify-end gap-2">
        <DialogCloseTrigger asChild>
          <Button variant="outline">Cancel</Button>
        </DialogCloseTrigger>
        <Button>Save</Button>
      </div>
    </DialogContent>
  </DialogPositioner>
</DialogRoot>

### Installation

<Tabs items={["bun", "pnpm", "yarn", "npm"]} className="mt-4">
  <Tab value="bun">
    ```
    bunx --bun shadcn@latest add http://localhost:3000/r/dialog.json
    ```
  </Tab>
  <Tab value="pnpm">
    ```
    pnpm dlx shadcn@latest add http://localhost:3000/r/dialog.json
    ```
  </Tab>
  <Tab value="yarn">
    ```
    npx shadcn@latest add http://localhost:3000/r/dialog.json
    ```
  </Tab>
  <Tab value="npm">
    ```
    npx shadcn@latest add http://localhost:3000/r/dialog.json
    ```
  </Tab>
</Tabs>

### Examples

Let's see how to use the `Dialog` components. Let's start with a basic example:

```tsx
import { Button } from "@/components/ui/button"
import {
  DialogRoot,
  DialogTrigger,
  DialogBackdrop,
  DialogPositioner,
  DialogContent,
  DialogTitle,
  DialogDescription,
  DialogCloseTrigger
} from "@/components/ui/dialog"

export const BasicDialog = () => (
  <DialogRoot>
    <DialogTrigger asChild>
      <Button>Open Dialog</Button>
    </DialogTrigger>
    <DialogBackdrop />
    <DialogPositioner>
      <DialogContent>
        <DialogCloseTrigger />
        <DialogTitle>Dialog Title</DialogTitle>
        <DialogDescription>This is a description of the dialog's purpose.</DialogDescription>
        <div className="py-4">This is the main content of the dialog.</div>
        <div className="flex justify-end gap-2">
          <DialogCloseTrigger asChild>
            <Button variant="outline">Cancel</Button>
          </DialogCloseTrigger>
          <Button>Save</Button>
        </div>
      </DialogContent>
    </DialogPositioner>
  </DialogRoot>
)
```

```tsx
import { Button } from "@/components/ui/button"
import {
  DialogRoot,
  DialogTrigger,
  DialogBackdrop,
  DialogPositioner,
  DialogContent,
  DialogCloseTrigger,
  DialogTitle,
  DialogDescription
} from "@/components/ui/dialog"

export const CustomDialog = () => (
  <DialogRoot>
    <DialogTrigger asChild>
      <Button variant="outline">Open Custom Dialog</Button>
    </DialogTrigger>
    <DialogBackdrop className="bg-black/40" />
    <DialogPositioner>
      <DialogContent className="border-primary/20 max-w-md">
        <DialogCloseTrigger className="bg-primary-50 rounded-full p-1" />
        <DialogTitle className="text-primary text-xl">Custom Dialog</DialogTitle>
        <DialogDescription className="text-base-600">
          You can customize the styling of each dialog component.
        </DialogDescription>
        <div className="border-t pt-4">
          <div>This dialog has custom styling applied to various parts.</div>
        </div>
        <div className="flex justify-end gap-2 mt-4">
          <DialogCloseTrigger asChild>
            <Button variant="outline">Dismiss</Button>
          </DialogCloseTrigger>
          <Button>Confirm</Button>
        </div>
      </DialogContent>
    </DialogPositioner>
  </DialogRoot>
)
```

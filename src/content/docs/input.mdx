---
title: Input
description: A form field for collecting user input.
---

import { Input } from "@/registry/thornberry/components/input";

<Input placeholder="Please enter text..." className="mb-8" />

### Installation

<Tabs items={["bun", "pnpm", "yarn", "npm"]} className="mt-4">
  <Tab value="bun">
    ```
    bunx --bun shadcn@latest add http://localhost:3000/r/input.json
    ```
  </Tab>
  <Tab value="pnpm">
    ```
    pnpm dlx shadcn@latest add http://localhost:3000/r/input.json
    ```
  </Tab>
  <Tab value="yarn">
    ```
    npx shadcn@latest add http://localhost:3000/r/input.json
    ```
  </Tab>
  <Tab value="npm">
    ```
    npx shadcn@latest add http://localhost:3000/r/input.json
    ```
  </Tab>
</Tabs>

### Examples

Let's see how to use the `Input` component. Let's start with a basic example:

```tsx
import { Input } from "@/components/ui/input"

export const BasicInput = () => (
  <Input placeholder="Please enter text..." />
)
```

You can add a label to your input:

```tsx
import { Input } from "@/components/ui/input"

export const LabeledInput = () => (
  <>
    <label htmlFor="email">Email</label>
    
    <Input id="email" type="email" placeholder="Enter your email" />
  </>
)
```

Input supports different input types:

```tsx
import { Input } from "@/components/ui/input"

export const InputTypes = () => (
  <>
    <Input type="text" placeholder="Text input" />
    
    <Input type="password" placeholder="******" />
    
    <Input type="email" placeholder="Email input" />
    
    <Input type="number" placeholder="Number input" />
    
    <Input type="search" placeholder="Search input" />
  </>
)
```

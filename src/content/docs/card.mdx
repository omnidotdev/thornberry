---
title: Card
description: Surface container for grouping related content and actions.
---

import { Button } from "@/registry/thornberry/components/button";
import { CardRoot, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from "@/registry/thornberry/components/card";

<CardRoot className="mb-8">
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>This is the card description.</CardDescription>
  </CardHeader>
  <CardContent>
    This card uses the primitives for maximum flexibility.
  </CardContent>
  <CardFooter>
    <Button>Save</Button>
  </CardFooter>
</CardRoot>

## Installation

import { InstallationTabs } from "@/components";

<InstallationTabs filename="card.json" />

## Usage

```tsx
import {
  CardRoot,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter
} from "@/components/ui/card";
```

```tsx
<CardRoot>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card Description</CardDescription>
  </CardHeader>
  <CardContent>
    Card Content
  </CardContent>
  <CardFooter>
    <Button>Action</Button>
  </CardFooter>
</CardRoot>
```

## Examples

<br />

#### Basic Card

<Tabs items={["Preview", "Code"]} className="mt-4">
  <Tab value="Preview">
    <CardRoot>
      <CardHeader>
        <CardTitle>Basic Card</CardTitle>
        <CardDescription>A simple card example</CardDescription>
      </CardHeader>
      <CardContent>
        This is the content area of the card, where the main information is displayed.
      </CardContent>
      <CardFooter>
        <Button>Save</Button>
      </CardFooter>
    </CardRoot>
  </Tab>
  <Tab value="Code">
    ```tsx
    import {
      CardRoot,
      CardHeader,
      CardTitle,
      CardDescription,
      CardContent,
      CardFooter
    } from "@/components/ui/card"
    import { Button } from "@/components/ui/button"

    export const BasicCard = () => {
      return (
        <CardRoot>
          <CardHeader>
            <CardTitle>Basic Card</CardTitle>
            <CardDescription>A simple card example</CardDescription>
          </CardHeader>
          <CardContent>
            This is the content area of the card, where the main information is displayed.
          </CardContent>
          <CardFooter>
            <Button>Save</Button>
          </CardFooter>
        </CardRoot>
      )
    }
    ```
  </Tab>
</Tabs>

#### Custom Styled Card

<Tabs items={["Preview", "Code"]} className="mt-4">
  <Tab value="Preview">
    <CardRoot className="border-primary/50 overflow-hidden">
      <CardHeader className="bg-muted/50">
        <CardTitle className="text-primary">Custom Styled Card</CardTitle>
        <CardDescription>With custom styling applied to each part</CardDescription>
      </CardHeader>
      <CardContent className="py-4 space-y-2">
        <p>You can customize each part of the card independently for complete control.</p>
        <p>Add any content you need here.</p>
      </CardContent>
      <CardFooter className="flex justify-between">
        <Button variant="outline">Cancel</Button>
        <Button>Submit</Button>
      </CardFooter>
    </CardRoot>
  </Tab>
  <Tab value="Code">
    ```tsx
    import {
      CardRoot,
      CardHeader,
      CardTitle,
      CardDescription,
      CardContent,
      CardFooter
    } from "@/components/ui/card"
    import { Button } from "@/components/ui/button"

    export const CustomStyledCard = () => {
      return (
        <CardRoot className="border-primary/50 overflow-hidden">
          <CardHeader className="bg-muted/50">
            <CardTitle className="text-primary">Custom Styled Card</CardTitle>
            <CardDescription>With custom styling applied to each part</CardDescription>
          </CardHeader>
          <CardContent className="py-4 space-y-2">
            <p>You can customize each part of the card independently for complete control.</p>
            <p>Add any content you need here.</p>
          </CardContent>
          <CardFooter className="flex justify-between">
            <Button variant="outline">Cancel</Button>
            <Button>Submit</Button>
          </CardFooter>
        </CardRoot>
      )
    }
    ```
  </Tab>
</Tabs>

## References

<div>
    <AutoTypeTable path="./src/content/docs/types.ts" name="CardProps" />
</div>
---
title: Switch
description: A control that allows the user to toggle between checked and unchecked states
---

import {
  Switch,
  SwitchContext,
  SwitchControl,
  SwitchHiddenInput,
  SwitchLabel,
  SwitchProvider,
  SwitchRoot,
  SwitchThumb,
} from "@/registry/thornberry/components/switch";

## Basic

```tsx
<Switch />
```

<Switch className="mb-4" />

## With Label

```tsx
<Switch label="Airplane Mode" />
```

<Switch label="Airplane Mode" className="mb-4" />

## Primitives

```tsx
<SwitchRoot defaultChecked>
  <SwitchHiddenInput />
  <SwitchControl className="bg-red-100 data-[state=checked]:bg-red-500">
    <SwitchThumb className="bg-white" />
  </SwitchControl>
  <SwitchLabel>Custom Colors</SwitchLabel>
</SwitchRoot>
```

<div className="flex flex-col gap-2">
    <SwitchRoot defaultChecked>
    <SwitchHiddenInput />
    <SwitchControl className="bg-red-100 data-[state=checked]:bg-red-500">
        <SwitchThumb className="bg-white" />
    </SwitchControl>
    <SwitchLabel>Custom Colors</SwitchLabel>
    </SwitchRoot>
    <SwitchRoot>
    <SwitchHiddenInput />
    <SwitchControl className="h-6 w-12">
        <SwitchThumb className="h-5 w-5 data-[state=checked]:translate-x-6" />
    </SwitchControl>
    <SwitchLabel>Custom Size</SwitchLabel>
    </SwitchRoot>
</div>

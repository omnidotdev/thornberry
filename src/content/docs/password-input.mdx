---
title: Password Input
description: A password input with show/hide toggle functionality.
---

import { PasswordInputRoot, PasswordInputLabel, PasswordInputControl, PasswordInputInput, PasswordInputVisibilityTrigger } from "@/registry/thornberry/components/password-input";

<PasswordInputRoot className="mb-8">
  <PasswordInputLabel>Password</PasswordInputLabel>
  <PasswordInputControl>
    <PasswordInputInput placeholder="Enter password" />
    <PasswordInputVisibilityTrigger />
  </PasswordInputControl>
</PasswordInputRoot>

## Installation

<InstallationTabs filename="password-input.json" />

## Usage

```tsx
import {
  PasswordInputRoot,
  PasswordInputLabel,
  PasswordInputControl,
  PasswordInputInput,
  PasswordInputVisibilityTrigger
} from "@/components/ui/password-input";
```

```tsx
<PasswordInputRoot>
  <PasswordInputLabel>Password</PasswordInputLabel>
  <PasswordInputControl>
    <PasswordInputInput />
    <PasswordInputVisibilityTrigger />
  </PasswordInputControl>
</PasswordInputRoot>
```

## Examples

<Tabs items={["Basic", "With Validation", "Disabled"]} className="mt-4">
  <Tab value="Basic">
    <PasswordInputRoot>
      <PasswordInputLabel>Password</PasswordInputLabel>
      <PasswordInputControl>
        <PasswordInputInput placeholder="Enter your password" />
        <PasswordInputVisibilityTrigger />
      </PasswordInputControl>
    </PasswordInputRoot>

    ```tsx
    <PasswordInputRoot>
      <PasswordInputLabel>Password</PasswordInputLabel>
      <PasswordInputControl>
        <PasswordInputInput placeholder="Enter your password" />
        <PasswordInputVisibilityTrigger />
      </PasswordInputControl>
    </PasswordInputRoot>
    ```
  </Tab>
  <Tab value="With Validation">
    <PasswordInputRoot invalid>
      <PasswordInputLabel>Password</PasswordInputLabel>
      <PasswordInputControl>
        <PasswordInputInput placeholder="Enter your password" />
        <PasswordInputVisibilityTrigger />
      </PasswordInputControl>
      <p className="mt-2 font-medium text-destructive text-sm">Password must be at least 8 characters</p>
    </PasswordInputRoot>

    ```tsx
    <PasswordInputRoot invalid>
      <PasswordInputLabel>Password</PasswordInputLabel>
      <PasswordInputControl>
        <PasswordInputInput placeholder="Enter your password" />
        <PasswordInputVisibilityTrigger />
      </PasswordInputControl>
      <p className="mt-2 font-medium text-destructive text-sm">
        Password must be at least 8 characters
      </p>
    </PasswordInputRoot>
    ```
  </Tab>
  <Tab value="Disabled">
    <PasswordInputRoot disabled>
      <PasswordInputLabel>Password</PasswordInputLabel>
      <PasswordInputControl>
        <PasswordInputInput placeholder="Password is disabled" />
        <PasswordInputVisibilityTrigger />
      </PasswordInputControl>
    </PasswordInputRoot>

    ```tsx
    <PasswordInputRoot disabled>
      <PasswordInputLabel>Password</PasswordInputLabel>
      <PasswordInputControl>
        <PasswordInputInput placeholder="Password is disabled" />
        <PasswordInputVisibilityTrigger />
      </PasswordInputControl>
    </PasswordInputRoot>
    ```
  </Tab>
</Tabs>

## References

<div>
  <AutoTypeTable path="./src/content/docs/types.ts" name="PasswordInputProps" />
</div>
